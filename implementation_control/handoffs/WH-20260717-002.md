# WH-20260717-002 Governed Continuation Skill Family Import

Git proves the Homepage action-row packet was checkpointed locally at
`b6508aca8932f45d9dc6ea4ecf4438ae922cfc95`. That checkpoint is not replayed.

The owner approved a separate tooling-only packet that imported the complete
`governed-continuation` bundle at source commit
`f2852d5273ed7297a0abd55c9aecfbf7f5e4507e`. The locked dependency closure is
`agentjob-control`, `continue`, `continue-goal`, and
`continue-implementing-goal`, all at version `0.1.0`.

The exact vendor copies are installed under `.agents/skills/` and remain
byte-identical to the source lock. Website-native Codex front doors, adapter
configuration, integrity validation, activation receipts, recovery, and
focused tests are integrated separately. Mutable relay state belongs under
ignored `.local/sys4ai/continuation/`.

All 286 vendored runtime tests, 18 focused relay tests, 181 website tests,
aggregate validation, quality, and the 64-page static build pass against the
accepted clean source root. The exact fixture and integration-document
resources required by the complete vendored suite are also source-pinned and
hash-verified.

`implementation_control/` remains the website's only implementation authority.
This packet forbids `.agents/control` bootstrap, public route or asset changes,
claim promotion, source refresh, real goal launch, real Codex task creation,
push, deployment, and upstream writes.

Completion `WJC-20260717-002-A` lists the exact 257 packet paths. The remaining
lawful action is the owner-authorized local checkpoint after its dry run proves
that all three unrelated untracked implementation plans remain excluded.
