# WH-20260712-008: FE-P1-01 Reactivated After Source Refresh

The owner authorized the bounded `FE-P1-01` z-index-token and native
dark-color-scheme packet after the `FE-P0-07` checkpoint at
`90e6c0a1e8e21ea7c63bb9bfe1085c69e90f3f6c`.

## Boundary

All 58 current `BaseLayout` consumers provide the dark
`project-overview-page` shell, while the root still advertises a light native
color scheme. The global stylesheet also contains six raw `z-index`
declarations across five established numeric layers.

This packet may add named z-index tokens, substitute the existing raw values,
align root CSS and BaseLayout metadata with the dark native scheme, add one
focused test module, and update only its control records.

## Stop Boundary

Skip-link visibility and focus behavior remain reserved for `FE-P1-02`.
Navigation behavior, content, claims, routes, manifests, SVG, motion, public
assets, upstream sources, push, and deployment remain unchanged.

## Resolved Validation Blocker

The bounded frontend implementation and browser review are complete. The
separate `FE-SR-01` packet refreshed the source-derived evidence, cleared the
curator drift, passed aggregate validation, and was checkpointed at
`dba66ec6ef2dc454aefe2666dfeb1955675b896a`.

Desktop, mobile, and expanded-dialog review confirms dark native controls,
legible shell and dialog contrast, and the preserved numeric order for figure
controls, skip link, navigation panel, and site header.

## Logical Next Step

Rerun `FE-P1-01` aggregate validation, create completion
`WJC-20260712-008-A`, and checkpoint the three preserved frontend files with
their allowlisted control records. Stop before `FE-P1-02`, push, or deployment.

The rerun passed, including 34-route provenance, current zero-drift curator
evidence, implementation-control validation, and a 64-page build. Completion
`WJC-20260712-008-A` is ready for the separate local checkpoint.
