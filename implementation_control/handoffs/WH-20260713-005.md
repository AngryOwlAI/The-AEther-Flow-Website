# WH-20260713-005

`FE-P1-06` opens only after live Git evidence showed that `TW-DESC-01` was
already locally checkpointed at
`efa78149f5fa6ef7521a0f20d58047938847c4d9`. The stale checkpoint transaction
was not replayed.

This packet creates `OverflowTable` and `TermCardGrid`, then applies each to the
existing `/physics/ontology/` route. Public vocabulary becomes one semantic
card list. The claim-boundary comparison keeps its table headers and row
semantics inside an instructed, focusable horizontal scroll region with
directional edge cues.

The existing frontend baseline inventory remains the classification input.
This bounded pilot does not migrate the other audited tables or regenerate the
baseline inventory. Existing reviewed ontology wording, route identity, source
bundles, navigation, public assets, and upstream authority remain unchanged.

Component-scoped styles keep the dirty `global.css` outside the packet. The
other seven unrelated dirty paths also remain byte-for-byte unchanged and
unstaged. No push or deployment is authorized.

## Completion

The ontology vocabulary now renders six terms as one labeled semantic card
list. Every card repeats the `Meaning here` and `Boundary` labels. The claim
boundary remains a single semantic table inside a labeled overflow region.

At 320 pixels the table region contains 704 pixels of table content within a
271-pixel viewport. Its instruction and end cue are visible. Keyboard `End`
and `Home` move across the full horizontal range, and keyboard focus shows a
three-pixel outline. The 360 by 225 high-zoom-equivalent review has no page
level horizontal overflow. Source Authority remains present after the pilot.

The three focused tests pass after the expected three-failure baseline. All
126 Python tests, the 64-page build, aggregate validation, and the quality gate
pass. Provenance retains 34 mapped routes, and curator evidence retains 248
dependencies with zero drift. Completion `WJC-20260713-005-A` is ready for the
separate local checkpoint. Do not push or deploy.
