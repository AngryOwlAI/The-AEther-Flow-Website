# WH-20260715-016 AgentJob Lifecycle Project Introduction

## Boundary

- `AI-WORKFLOW-INTRO-01` is checkpointed at `b1c7b5bd785c5137c5f0a750c361e6b471662208` and is not replayed.
- This packet replaces only the `/ai-research-system/agentjob-lifecycle/` raw topic paragraph with one source-grounded whole-project introduction immediately after its hero.
- Envelope rows, lifecycle rows, proof rows, source rows, route cards, diagrams, and shared styles remain unchanged.
- The dirty live upstream worktree is preserved outside the packet; the accepted clean pin is the only upstream source basis.
- Sibling routes, Resources routes, source refresh, upstream writes, push, and deployment remain forbidden.

## Required outcome

1. One substantial project-description paragraph follows the AgentJob Lifecycle hero.
2. The paragraph identifies the two-part project before narrowing to one AgentJob.
3. Exact GR remains the observable benchmark while the proposed foundation and first-principles derivation remain open.
4. One AgentJob is explained as a single-use executable permission envelope with concrete inputs, allowlists, limits, outputs, validators, and stop conditions.
5. Director decision, execution role, completion, handoff, and checkpoint narrow and preserve one transaction.
6. AgentJobs, validator PASS, completions, handoffs, and checkpoints remain transaction evidence rather than proof, upstream authority, or reusable permission.
7. Writing, focused tests, provenance, build, aggregate quality, and browser review pass.

## Result

- The raw AgentJob Lifecycle topic paragraph is replaced by one substantial general-public project introduction immediately after the hero.
- The paragraph defines the two-part program before narrowing to one single-use AgentJob permission envelope and its record chain.
- AgentJobs, validator PASS, completions, handoffs, and checkpoints remain transaction evidence rather than physics proof, upstream authority, or reusable permission.
- Envelope rows, lifecycle rows, proof rows, source rows, route cards, diagrams, and shared styles remain unchanged.
- The technical-writing gate passed with zero warning matches.
- Seventeen focused positioning tests and all 142 Python tests passed.
- Provenance validates for 34 routes, and the static build produces 64 pages.
- Aggregate validation and the quality gate passed against the accepted clean source pin.
- Desktop and compact in-app browser review passed without horizontal overflow or console errors.
- The dirty live upstream worktree remained untouched and outside the source basis.

## Next action

Run the local checkpoint transaction for `WJC-20260715-016-A` and stop without entering another route.
