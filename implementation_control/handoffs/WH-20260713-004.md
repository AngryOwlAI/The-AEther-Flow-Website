# WH-20260713-004

`TW-DESC-01` opened only after live Git evidence showed that `FE-SR-04` was
already locally checkpointed at
`f440ce4a412659997b8ab5ee44f445dc98a43d54`. The resolver instruction to run
that checkpoint transaction again was stale and was not replayed.

This packet is limited to one explicit Description section immediately after
the complete hero on the operations landing and its six child routes. It reuses
each page's existing reviewed metadata description and keeps Source Authority
as the final page-owned section. Focused structural tests, generated provenance,
curator evidence, and exact control records are included.

The separate parent-child explainer, route identity, navigation, redirects,
source bundles, scientific state, public assets, upstream files, push, and
deployment remain outside the packet. Eight unrelated dirty website paths must
remain byte-for-byte unchanged and unstaged.

## Completion

All seven operations routes now opt in to a shared Description section that
renders each page's existing reviewed metadata description immediately after
the complete hero. Source Authority remains the final page-owned section. The
separate parent-child explainer remains outside the opt-in contract.

Focused structural tests pass, the full Python suite passes 123 tests, all 64
static pages build, aggregate validation passes, and the quality gate passes.
Provenance regeneration retains 34 mapped canonical routes; because the seven
legacy operations routes are not route-map entries, their generated record set
does not change. Curator evidence retains 248 dependencies with zero drift or
diagnostics. The packet is ready for its separate local checkpoint.
