# WH-20260715-002

The owner-authorized accessibility evidence amendment is separately
checkpointed at `d6d95ceacda0bb91dae0e3994cbd92f16048f3a4`. The amended policy
retains the WCAG 2.2 Level AA target, VoiceOver with Safari, keyboard, browser,
zoom, touch, no-JavaScript, automated regression, and complete-process
evidence. This packet does not replay that checkpoint.

`FE-P1-03R` reaccepts the preserved compact mobile navigation implementation
under the amended required matrix. The original cancellation remains an
unchanged historical record. The current packet may consume only
`BaseLayout.astro`, `global.css`, `tests/test_mobile_navigation.py`, five
preserved FE-P1-02 control-reconciliation paths, and its exact new control
records.

Acceptance requires focused and full Python tests, the static build, aggregate
validation, the quality gate, pointer and keyboard interactions, Escape focus
return, outside click, nested group and current-route behavior, resize and
orientation handling, 320-pixel and short-height profiles, 200 and 400 percent
zoom, no-JavaScript destinations, and VoiceOver/Safari state and focus-order
review.

Do not change navigation labels, destinations, groups, routes, desktop target
sizes, claims, manifests, assets, historical cancellation records, upstream
sources, remote Git state, or deployment state.

## Completion

The compact navigation implementation and the five reconciliation records are
preserved in interim review commit
`651eadc2f3d63283d9c0dbf039b82c84f1d50282`. The focused and full automated
tests, headed Chromium interaction checks, responsive and zoom-equivalent
layouts, current-route behavior, and no-JavaScript fallback passed before the
final assistive-technology review.

On macOS 26.5.2 with Safari 26.5.2, VoiceOver exposed the compact control as
`Menu`, reported its collapsed and expanded states, and exposed the enclosing
`Primary navigation` landmark. Control-Option-Space activated the focused Menu
and Physics Research disclosures. Safari focus proceeded from Menu to Home to
Physics Research and then through Physics Overview and Ontology inside the
open nested group. Escape collapsed the compact menu and returned focus to the
collapsed Menu button. VoiceOver was disabled after the review.

All 129 Python tests, the 64-page static build, aggregate validation, and the
quality gate pass with `AETHER_FLOW_SOURCE_ROOT` bound to a clean disposable
clone at accepted source pin
`57438af555214bc0785dcb390ee6254f580b8a62`. The initial unbound quality
diagnostic read unrelated live upstream drift; it did not alter the upstream
worktree and is superseded by the required pinned validation. Completion
`WJC-20260715-002-A` is ready for the bounded local checkpoint. Do not push,
deploy, open FE-P1-04, or mutate upstream.
