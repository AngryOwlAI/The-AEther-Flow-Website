# WH-20260715-003 - FE-P1-04 Desktop Navigation Targets

Status: active and ready for local checkpoint

FE-P1-03R is complete at `7db8eb09654ce7b3b664725763aa21c8cb6aeafc`.
This fresh owner-authorized packet does not replay that checkpoint. It opens
`FE-P1-04` only after observing the completed dependency as `HEAD`.

## Bounded implementation

Added one non-compact navigation rule that gives existing top-level desktop links
and disclosure triggers a 44 CSS pixel minimum target height. Preserve labels,
destinations, grouping, current-page state, Escape close and focus return,
outside-click handling, nested disclosure behavior, and the compact mobile Menu.

Focused static regression checks and headed browser evidence verified computed target dimensions,
current state, Escape closing, and focus return in a headed desktop browser.

At 1440 by 900 all four desktop controls measured exactly 44 CSS pixels high.
The Physics group remained active, the Ontology child retained
`aria-current="page"`, and Escape collapsed the group with focus returned to its
trigger. At the compact 780 pixel breakpoint the existing 32 CSS pixel inner
targets remained unchanged. Four focused tests, all 130 Python tests, the 64
page build, aggregate validation, and the quality gate passed against the
accepted clean source pin.

## Boundaries

No navigation content, route identity, claim, manifest, asset, upstream source,
push, or deployment change is authorized. Stop after one validated local
checkpoint.

The logical next step is the automated local checkpoint for
`WJC-20260715-003-A`. No push or deployment is authorized.
