# WH-20260630-043: Diagram Gallery Quality-Gate Contract

This packet authorizes a validator-contract fix for the release-candidate QA
blocker where `/resources/diagrams/` is checked against the legacy
resource-support schema even though the greenfield route intentionally uses the
command-band and track-page layout.

## Boundary

GF-014 follow-up may change:

- `scripts/quality_gate.py`
- `tests/test_quality_gate.py`
- `docs/quality/sitewide-greenfield-release-candidate-report.md`
- `docs/quality/sitewide-greenfield-review-ledger.md`
- this packet's implementation-control records

It must not change public route content, route structure, manifests, source
snapshots, generated derivatives, public assets, visual systems, dependencies,
deployment state, or upstream source files.

## Required Validation

- `git diff --check`
- `python3 scripts/quality_gate.py`
- `npm run validate:implementation-control`
- `.venv/bin/python -m pytest`
- `npm run build`
- Manual control-record inspection

## Completion Evidence

Completion evidence is recorded at
`implementation_control/tasks/WI-20260630-043/jobs/completions/WJC-20260630-043-A.yaml`.
The quality gate now treats `/resources/documents/` as the support-schema
route and `/resources/diagrams/` as a greenfield Diagram Gallery route.
`python3 scripts/quality_gate.py`, `.venv/bin/python -m pytest`, `npm run
build`, `npm run validate:implementation-control`, and `git diff --check`
passed.

## Remaining Separate Blocker

Curator/source-drift remains separate and must not be silently folded into this
validator-contract packet.
This packet is superseded by `WI-20260630-044` for website-side source-derived
artifact refresh.
