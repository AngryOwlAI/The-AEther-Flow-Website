# WH-20260714-001

`FE-P1-07` opens only after live Git evidence showed that `FE-P1-06` was
already locally checkpointed at
`9f4a6b41d2aeac73c781341344fa43481f19effb`. The stale checkpoint transaction
was not replayed.

This packet adds intrinsic dimensions, stable aspect ratios, and an explicit
loading policy to the shared Figure, comprehension-diagram, and diagram-gallery
surfaces. The existing approved PNG bytes and public wording remain unchanged.
Below-fold comprehension diagrams become lazy by default, while the existing
first-view Figure consumers retain explicit eager loading.

The packet may add central image metadata, focused regression coverage,
regenerated page provenance, curator evidence, and exact completion records.
It may not modify public assets, route identity, route maps, navigation,
`global.css`, the preserved mobile-navigation work, upstream sources, remote
Git state, or deployment state.

Eight unrelated dirty paths remain byte-for-byte outside the packet and must
remain unstaged. `FE-P1-03` remains cancelled and `FE-P1-04` remains blocked.
No push or deployment is authorized.

## Completion

All 37 approved comprehension PNGs now have byte-verified intrinsic dimensions
and derived aspect ratios in one metadata contract. `Figure` applies the
geometry to its primary and expanded-dialog images. `ComprehensionBlocks`
defaults to lazy loading, while the existing first-view Figure consumers retain
their explicit eager policy. Gallery images remain lazy and reserve a bounded
aspect-ratio frame before request dispatch.

A fresh headed-browser observation initially deferred 36 of 37 gallery images.
The selected far-below-fold image had an 804.8 by 211.5 pixel frame before its
request, loaded when scrolled into view, retained the same frame, and produced
zero layout-shift entries. A fresh full-load observer also recorded zero layout-
shift entries, and all 37 gallery images exposed width, height, and aspect ratio.

The three focused tests pass after the expected three-failure baseline. All 129
Python tests, the 64-page build, aggregate validation, and the quality gate pass.
Provenance retains 34 mapped routes. Curator evidence retains 248 dependencies
with zero drift or diagnostics. Completion `WJC-20260714-001-A` is ready for the
separate local checkpoint. Do not push or deploy.
