# WH-20260715-032 Desktop Navigation Link Flex Alignment Repair

## Boundary

- Reading Paths is checkpointed at `095c6e66c961a8f581c818dc48a217e4a3d6866b` and is not replayed.
- This packet owns only the existing `global.css` navigation-selector change, its focused test assertion, and the control transition.
- The original 44 CSS pixel desktop target packet remains checkpointed at `529bc34aa975c099ea0b545aaccd25654b13d0f6`.
- Introductions, claims, routes, manifests, assets, sources, push, and deployment remain outside this packet.

## Required outcome

1. Direct top-level navigation links center their content inside the existing 44 CSS pixel desktop target.
2. Disclosure targets retain the same alignment and sizing.
3. Compact targets remain 32 CSS pixels high.
4. Focused tests, full validation, quality, and browser review pass.

## Next action

Run the local checkpoint for completed `WJC-20260715-032-A`, then stop before opening the site-wide introduction packet.

## Completion evidence

- The focused navigation test file passed with four tests; the full Python suite passed with 156 tests.
- The static build produced 64 pages; aggregate validation and the quality gate passed against the accepted clean source pin.
- Desktop review measured all four top-level targets at 44 CSS pixels with flex centering and preserved the active Resources state.
- Compact review measured all four targets at 32 CSS pixels with flex centering and zero horizontal overflow.
