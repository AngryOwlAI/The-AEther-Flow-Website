# WH-20260715-030 Home Hero Actions And Status Placement

## Boundary

- `RESOURCES-LIBRARY-INTRO-01` is checkpointed at `6e4019da2aacde200c627345203a0c0350973813` and is not replayed.
- This packet moves only existing Home elements; it does not change copy, links, classes, status data, sources, shared styles, or components.
- The live upstream worktree is dirty with unrelated in-flight work and remains read-only.
- Reading Paths remains paused and outside this packet.
- Push and deployment remain forbidden.
- The full suite exposed one obsolete status-before-positioning assertion. Its focused test file was added to this packet and corrected without widening production scope.

## Required outcome

1. The existing Ontology and GR, Physics, AI Research, and Resources actions render inside the Home hero.
2. The separate main-level action row is removed.
3. The unchanged Current Project Status section follows the route chooser headed `Choose the route that matches your question.`
4. Focused order tests, provenance, full validation, quality, and desktop plus compact browser review pass.

## Next action

Run the local implementation checkpoint transaction and stop without push or deployment.

## Completed evidence

- The four primary actions render inside the Home hero at desktop and compact widths.
- The unchanged five-state list is the immediate next section after the route chooser.
- Focused tests passed 35 checks; the full Python suite passed 155 checks.
- Aggregate validation, the 64-page build, and the quality gate passed against the accepted clean source pin.
- Desktop and browser-minimum compact review found zero horizontal overflow.
