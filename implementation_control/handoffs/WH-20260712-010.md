# WH-20260712-010: FE-P1-02 Skip-Link And Main-Focus Packet

`FE-P1-01` is completed and locally checkpointed at
`4f72bb453f233208ea2ba1bbf16e176d41de35c8`. The checkpoint is an ancestor of
current `HEAD` and must not be repeated.

## Boundary

The owner separately authorized `FE-P1-02`. The packet may raise the existing
skip-link layer above the site header, make `main` an explicit focus target,
provide an unambiguous focus/reading position, update one focused existing test
file, and maintain its allowlisted implementation-control records.

The packet must preserve header-menu behavior, navigation target sizes, content,
claims, routes, manifests, SVGs, motion, public assets, and the upstream source
project.

## Required Review

Verify the first Tab and Enter sequence at desktop and 320px widths, at 200 and
400 percent browser zoom, and with header menus open and closed. The focused
skip link must remain visibly above the header, and activation must focus and
position `main` unambiguously.

## Stop Boundary

Stop after one validated local `FE-P1-02` checkpoint. Do not open `FE-P1-03`,
push, deploy, promote physics, or mutate the upstream source repository.

## Completion State

The bounded patch is complete. The skip link resolves to layer `60` above the
header at `50`; first Tab reveals it at desktop and 320px widths; and Enter
focuses `#main-content` with a visible 3px outline. Closed- and open-menu cases
passed at 100% and 200% browser scale. The 400% reflow case passed using a
1440×900 physical canvas represented by a 360×225 CSS viewport at device scale
4 because Chromium's page-scale automation interface caps at 300%.

All 109 Python tests, the 64-page build, aggregate validation, implementation-
control validation, and `git diff --check` pass. Completion
`WJC-20260712-010-A` is ready for the local checkpoint transaction.
