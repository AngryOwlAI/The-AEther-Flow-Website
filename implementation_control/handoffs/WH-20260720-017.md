# Handoff WH-20260720-017

Plan task `AFDOC-017` maps to website task `WI-20260720-017` and job `WJ-20260720-017-A`.

The bounded packet is complete. The primary navigation now exposes Documents
with exactly these internal category links in order: Documentation Overview,
Anthology Articles, Research Articles, Governance & Control, and Diagram
Gallery. The `/documents/` match prefix keeps the parent active across nested
Documents routes.

The shared header controller was not modified, so its desktop, compact,
keyboard, no-script, Escape, outside-click, and focus-return contracts remain
on the previously tested path. All six focused navigation tests, manual source
inspection, and `git diff --check` passed. No branch, worktree, staging, commit,
push, deployment, or upstream write was performed.

This handoff is continuity evidence only. It grants no authority beyond the
hash-bound YAML packet, and only the coordinator may reserve a successor.
