# WH-20260715-015 AI Research System Workflow Project Introduction

## Boundary

- `AI-CURRENT-STATE-INTRO-01` is checkpointed at `6f7b915c9fab0040528dbd0303f8ecd1df424196` and is not replayed.
- This packet replaces only the `/ai-research-system/workflow/` raw topic paragraph with one source-grounded whole-project introduction immediately after its hero.
- Lifecycle rows, boundary tables, route cards, diagrams, source authority, and shared styles remain unchanged.
- The dirty live upstream worktree is preserved outside the packet; the accepted clean pin is the only upstream source basis.
- Sibling routes, Resources routes, source refresh, upstream writes, push, and deployment remain forbidden.

## Required outcome

1. One substantial project-description paragraph follows the Workflow hero.
2. The paragraph identifies the two-part project before narrowing to the governed workflow.
3. Exact GR remains the observable benchmark while the proposed foundation and first-principles derivation remain open.
4. One request narrows through tracked state, source inspection, a Director decision, one AgentJob, completion, handoff, and checkpoint.
5. Memory, roles, child analyses, validators, completions, handoffs, and commits remain audit evidence rather than proof or expanded authority.
6. Protected human decisions remain outside automated execution.
7. Writing, focused tests, provenance, build, aggregate quality, and browser review pass.

## Result

- The raw Workflow topic paragraph is replaced by one substantial general-public project introduction immediately after the hero.
- The paragraph defines the two-part program before narrowing to tracked state, source inspection, a Director decision, one AgentJob, completion, handoff, and checkpoint.
- Memory, role labels, child analyses, validator PASS, completions, handoffs, and commits remain audit evidence rather than physics proof or expanded authority.
- Lifecycle rows, boundary tables, and workflow mechanics remain unchanged.
- The technical-writing gate passed with zero warning matches.
- Sixteen focused positioning tests and all 141 Python tests passed.
- Provenance validates for 34 routes, and the static build produces 64 pages.
- Aggregate validation and the quality gate passed against the accepted clean source pin.
- Desktop and compact in-app browser review passed without horizontal overflow or console errors.
- The dirty live upstream worktree remained untouched and outside the source basis.

## Next action

Run the local checkpoint transaction for `WJC-20260715-015-A` and stop without entering another route.
