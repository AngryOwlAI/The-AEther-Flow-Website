# WH-20260715-035 Sitewide Introduction Left Alignment

Git proves that `SITEWIDE-PROJECT-INTRO-PARAGRAPH-REGROUPING-01` was checkpointed at `96d50ae08fa5803ae7820c11b795b57f28f0ba32`; the stale checkpoint action was not replayed.

`SITEWIDE-PROJECT-INTRO-LEFT-ALIGNMENT-01` is active as `WI-20260715-035` and `WJ-20260715-035-A`. The packet is limited to changing the two existing shared introduction paragraph margins from centered columns to inline-start anchored columns, updating the focused positioning contract, and validating all 29 routes at desktop and 520 CSS pixel widths.

Copy, routes, component interfaces, panel geometry, max width, typography, paragraph gaps, compact rules, tracked manifests, public assets, source refresh, upstream writes, checkpoint, push, and deployment remain forbidden.

## Completion evidence

- Both `.home-intro-panel p` and `.greenfield-intro-panel p` now use `margin-inline: 0 auto` while retaining `max-width: 72ch`, left text alignment, existing typography, and 16 pixel paragraph gaps.
- Focused positioning coverage passed 33 tests and the full Python suite passed 158 tests.
- Manifest, claim, content, provenance, implementation-control, accepted-root aggregate, build, and quality gates passed; Astro built 64 static pages.
- Automated browser review passed all 29 routes at 1440 CSS pixels and all 29 routes at 520 CSS pixels. The maximum desktop paragraph-to-panel content-edge delta was 0.003125 CSS pixels, the compact delta was zero, and horizontal overflow was zero.
- The default aggregate run stopped at unrelated live-upstream curator drift. No source refresh or acknowledgement is authorized by this packet.

Completion `WJC-20260715-035-A` is ready for a separately authorized local checkpoint. Nothing was staged, committed, pushed, deployed, refreshed, or written upstream.
