# Handoff WH-20260720-018

Plan task `AFDOC-018` maps to website task `WI-20260720-018` and job `WJ-20260720-018-A`.

The bounded packet is complete. Primary reader journeys now point to canonical
Documents routes instead of legacy Resources redirect targets. Public Resources
and Library product labels were replaced while legitimate generic references to
resources remain intact. GitHub provenance links were preserved.

Both focused internal-link regression tests, `npm run validate:links`, and
`git diff --check` passed. No branch, worktree, staging, commit, push,
deployment, or upstream write was performed.

This handoff is continuity evidence only. It grants no authority beyond the
hash-bound YAML packet, and only the coordinator may reserve a successor.
