# WH-20260630-044: Curator Source Drift Refresh

This packet authorizes a website-local source-derived artifact refresh for the
remaining release-candidate blocker. It may update page provenance, the
checked-in current-state snapshot, curator reports, and curator routing/tests
using a clean committed upstream source view.

## Boundary

Allowed writes are limited to:

- `scripts/run_curator.py`
- `tests/test_curator_reports.py`
- `tests/test_curator_acknowledgements.py`
- `public/files/manifests/page_provenance.json`
- `src/data/physics_current_state_snapshot.json`
- `curator/reports/latest.json`
- `curator/reports/latest.md`
- `docs/quality/sitewide-greenfield-release-candidate-report.md`
- `docs/quality/sitewide-greenfield-review-ledger.md`
- this packet's implementation-control records

The packet must not write to the upstream source repository, use upstream dirty
working-tree content as committed evidence, change public route copy, mutate
public assets, regenerate diagrams, add dependencies, push, deploy, or mark
owner acceptance.

## Required Validation

- `git diff --check`
- `npm run validate`
- `npm run quality`
- `npm run validate:implementation-control`
- `.venv/bin/python -m pytest`
- Manual control-record inspection

## Completion Evidence

Completed in
`implementation_control/tasks/WI-20260630-044/jobs/completions/WJC-20260630-044-A.yaml`.

Evidence:

- Source-derived artifacts were regenerated from clean committed upstream source
  commit `2a934c29b58e84aa913a5088a8388bd259f6370b`.
- `npm run validate:curator` passed with `AETHER_FLOW_SOURCE_ROOT` bound to
  the clean committed source view.
- `npm run validate` passed with `AETHER_FLOW_SOURCE_ROOT` bound to the clean
  committed source view.
- `npm run quality` passed with `AETHER_FLOW_SOURCE_ROOT` bound to the clean
  committed source view.
- `.venv/bin/python -m pytest` passed.
- `python3 scripts/smoke_test_site.py --base-url http://127.0.0.1:4321`
  passed for 92 routes.
- `git diff --check` passed.
- `npm run validate:implementation-control` passed.
- Owner review remains pending. Deployment remains unauthorized.
