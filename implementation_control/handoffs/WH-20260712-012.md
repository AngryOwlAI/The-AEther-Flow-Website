# WH-20260712-012: FE-SR-02 Curator Source-Refresh Packet

The owner authorized one fresh bounded website-local curator source-refresh
repair. `WI-20260712-011` and `WJ-20260712-011-A` are suspended while this
packet executes. Their mobile-navigation runtime changes remain uncommitted and
must not enter the source-refresh checkpoint.

## Boundary

`FE-SR-02` may review curator-reported dependency drift from one clean committed
upstream view, refresh the claim-boundary and physics current-state snapshots,
regenerate page provenance, write curator JSON and Markdown reports, make only
directly required focused snapshot-generator and test repairs, and maintain its
allowlisted implementation-control records.

It must not create acknowledgements, change authored public copy, alter the
route map or source bundles, modify frontend runtime or public assets, promote
physics, write upstream, push, or deploy.

## Preflight State

At transition time the upstream `main` checkout is dirty and ahead of
`origin/main`. The control records are approved and resolver-ready, but source
refresh execution must stop until upstream `main` is clean and equals
`origin/main`. No dirty upstream bytes may be used as website evidence.

## Preserved Work

The uncommitted `FE-P1-03` changes in `BaseLayout.astro`, `global.css`, and
`tests/test_mobile_navigation.py` remain preserved outside this packet. The
source-refresh checkpoint must exclude those files.

After `FE-SR-02` is separately checkpointed, execute the already authorized
Windows/NVDA deferral as a distinct governance amendment before resuming
`FE-P1-03` acceptance. Do not open `FE-P1-04` in this packet.

## Execution Result

Upstream `main` became clean and matched `origin/main` at
`b8c4d7c6567fbf503b4f6dc7c044433bae2c02e5`. Review of the committed delta
found project-system and control material only: no ontology, TeX,
Distance-to-GR, program-state, frontier, or research-handoff change.

The claim-boundary snapshot, physics current-state snapshot, page provenance,
and curator reports were regenerated from that exact commit. Physics semantic
fields remain byte-equivalent as structured values. Seven new claim-boundary
rows describe v19 project-system work and explicitly forbid physics-proof,
adoption, benchmark-promotion, Gate Chair, and completed-derivation overreads.

Curator preview and validation report 248 dependencies, zero drift, zero
diagnostics, and zero review-required items. Page provenance retains all 34
route identities, route order, and local page hashes while repinning source
links to the accepted commit. The excluded `FE-P1-03` runtime file hashes are
unchanged.

All required validators passed, including 112 Python tests, a 64-page static
build, aggregate validation, and the quality gate. Completion
`WJC-20260712-012-A` is ready for a separate local checkpoint. No
acknowledgement, authored public copy, frontend runtime, upstream write,
physics promotion, push, or deployment occurred.

## Supersession

Completed and checkpointed at
`567a2d7544ac636c8a35683d4694f0bca86130b3`. Superseded by
`WH-20260712-013` after the owner cancelled `FE-P1-03` and opened
dependency-ready `FE-P1-05`.
