# WH-20260717-001 Homepage Primary Action Alignment

Git proves that `DIAGRAM-GALLERY-CONTAINMENT-01` was checkpointed at `3a2c8560f8f98d918a388677242b901c8e36a6f9`. That checkpoint is not replayed.

The owner requested that the Homepage primary action component align like the Physics overview action component. Browser geometry at a 1533 by 1285 CSS pixel viewport measured the Homepage command copy at x 166.667 with width 528.073, but the action row at x 182.667 with width 496.073. The Physics command copy and action row both begin at x 166.667 and both measure 528.073.

The failure is isolated to the Homepage row's two-rem width subtraction and centered horizontal margin. This packet permits one base `.home-action-row` spacing correction, one focused regression test, control records, and responsive browser verification. Route source, action labels, destinations, order, assets, manifests, provenance, source refresh, checkpoint, push, deployment, and upstream writes remain outside scope.

The row now fills the same command-copy containing block as the Physics overview actions and uses the same 1.7 rem top spacing. At the 1533 CSS pixel desktop reference, both Homepage and Physics copy/action pairs begin at x 166.667 and measure 528.073 pixels. The same edge alignment held at measured widths 1040, 780, and 520 with zero horizontal overflow and preserved action grouping.

The focused contract first failed on the old width and margin declarations, then passed after the correction. All 37 public positioning tests and all 163 Python tests passed. The accepted-source-root aggregate validation, quality gate, 64-page static build, and browser console review also passed. The packet is complete and unstaged pending separate local checkpoint authorization; push and deployment remain unauthorized.

The checkpoint dry run resolved exactly 12 packet paths, staged nothing, and excluded the unrelated untracked documents-navigation implementation plan.
