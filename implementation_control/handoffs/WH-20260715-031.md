# WH-20260715-031 Reading Paths General Public Project Introduction

## Boundary

- `HOME-HERO-ACTIONS-STATUS-ORDER-01` is checkpointed at `e90e1dded11102baa9f5fae556b3e1ffb11588b8` and is not replayed.
- This packet changes only the Reading Paths project introduction, focused tests, provenance, and implementation-control records.
- The owner made preflight failures nonblocking; the accepted clean source pin remains the writing basis.
- The live upstream worktree is dirty with unrelated in-flight work and remains read-only.
- Repository Map and all later route introductions remain outside this packet.
- Push and deployment remain forbidden.

## Required outcome

1. One substantial general public whole-project paragraph follows the Reading Paths hero.
2. The paragraph connects the physics and governed AI research tracks to question-first internal reading routes and source-first audit.
3. The existing topic-only introduction is removed.
4. Tables, links, diagrams, route clusters, policies, shared styles, and other routes remain unchanged.
5. Writing gate, focused tests, provenance, full validation, quality, and desktop plus compact browser review pass.

## Next action

Run the local implementation checkpoint and stop without push or deployment.

## Completion evidence

- The Reading Paths route now uses one `ProjectIntroduction` immediately after the hero and before the existing comprehension blocks.
- The technical-writing quality gate passed against the accepted clean source pin with zero warning matches.
- Focused tests passed with 31 tests; the full Python suite passed with 156 tests.
- Claim, content, provenance, build, aggregate validation, and quality gates passed; 64 static pages built and 34 provenance routes validated.
- Desktop and 520 CSS pixel compact browser review confirmed one readable paragraph, correct section order, and zero horizontal overflow. The browser binding exposed no stored console history, and no visible runtime error surface was observed.
- Reading Paths tables, links, diagram, route clusters, policies, shared styles, other routes, upstream sources, push, and deployment remain unchanged.
