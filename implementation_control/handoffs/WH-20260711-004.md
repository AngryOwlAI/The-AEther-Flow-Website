# WH-20260711-004: FE-G0-03 Performance Baseline Complete

Superseded by `WH-20260711-005` after separate owner authorization of
`FE-G0-04`.

The owner explicitly authorized `FE-G0-03` after the local `FE-G0-02`
checkpoint at `086d5f1364c759cffa4713e0e451dde217b8b49b`.

## Completed Packet

- Plan packet: `FE-G0-03`.
- Task: `WI-20260711-004`.
- Job: `WJ-20260711-004-A`.
- Completion: `WJC-20260711-004-A`.
- Outcome: three pinned mobile Lighthouse runs per required route establish
  evidence-qualified provisional budgets and exact baseline defects.

## Key Evidence

- CLS and TBT were zero in all 15 runs.
- Four routes failed the 4-second maximum-run LCP stability guard.
- The shared 1,147,309-byte PNG dominates initial transfer.
- Every representative route eagerly fetched below-fold images.
- Lighthouse reported unattributable long tasks up to 54 ms.
- No field INP or 75th-percentile field Core Web Vitals data was available.

## Verification

- Focused performance tests: 3 passed.
- Full Python suite: 82 passed.
- Static build: 64 pages built.
- Implementation-control validation: passed.
- `git diff --check`: passed.
- Aggregate validation: stopped at pre-existing stale curator reports and
  unacknowledged upstream drift after earlier validators passed.

## Boundary

The packet may write only its Markdown and JSON baseline, focused generator and
test, and exactly allowlisted control records. It may not alter frontend runtime
code, styles, public routes, redirects, navigation, copy, metadata, assets,
manifests, upstream source state, remote Git state, or deployment state.

## Logical Next Action

Create the local `FE-G0-03` checkpoint and stop. The owner may separately
authorize `FE-G0-04`; this handoff does not open it.
