# WH-20260712-011: FE-P1-03 Compact Accessible Mobile Menu Packet

`FE-P1-02` is completed and locally checkpointed at
`59e0b4128237f68ef839a439a50a09ae109dff90`. The checkpoint is current `HEAD`
and must not be repeated.

## Boundary

The owner separately authorized `FE-P1-03`. The packet may add one initially
collapsed mobile Menu control around the existing navigation data, preserve
nested group behavior, add viewport-safe internal scrolling, retain essential
top-level destinations without JavaScript, update one focused test module, and
maintain its allowlisted implementation-control records.

The packet must preserve navigation labels, destinations, groups, routes,
desktop target sizes, content, claims, manifests, SVGs, motion, public assets,
and the upstream source project.

## Required Review

Verify pointer and keyboard open and close, Escape with focus return, outside
click, nested groups, current-child state, 320px and short-landscape profiles,
orientation changes, 200 and 400 percent zoom, no-JavaScript destinations,
macOS VoiceOver with Safari, and Windows 11 NVDA with Firefox plus focused
Chrome interoperability.

## Stop Boundary

Stop after one fully validated local `FE-P1-03` checkpoint. If VoiceOver or
Windows NVDA evidence is unavailable, leave the packet uncheckpointed and
record the exact acceptance blocker. Do not open `FE-P1-04`, push, deploy,
promote physics, or mutate the upstream source repository.

## Implementation State

The bounded runtime patch is implemented in `BaseLayout.astro`, `global.css`,
and one focused test module. Chromium review passed pointer, Enter, Space,
Escape with Menu-focus return, outside click, one-open nested groups,
current-child state, 320px, 844 by 390 short landscape, resize, hidden-focus
safety, 200-percent-equivalent, 400-percent-equivalent, and JavaScript-disabled
cases. Both the outer compact surface and nested panels scroll internally, and
the repaired 400-percent-equivalent case keeps one full focused item visible.

All 112 Python tests, the focused foundation tests, the 64-page static build,
implementation-control validation, and `git diff --check` pass.

## Acceptance Blockers

`npm run validate` stops at stale curator reports and unacknowledged upstream
source drift, including critical drift for `/physics/claim-status/`. That repair
is outside this packet and requires separate owner authorization.

VoiceOver was not run because enabling it changes a local system setting and
requires explicit action-time confirmation. Windows 11 NVDA with Firefox and
focused Chrome interoperability is unavailable because this Mac has no Windows
runtime or virtual machine. The dated decision ledger requires both reviews and
does not allow automated accessibility snapshots to substitute for them.

No completion record or checkpoint was created. The implementation remains
preserved and uncheckpointed until the curator and assistive-technology gates
are cleared.

## Suspension

On 2026-07-12 the owner authorized a fresh bounded website-local curator
source-refresh repair. `WI-20260712-011` and `WJ-20260712-011-A` are suspended
while `WI-20260712-012` executes. The mobile-navigation runtime changes remain
uncommitted and must not enter the source-refresh checkpoint.

After the source-refresh packet is separately checkpointed, record the already
authorized Windows/NVDA deferral in its own governance amendment before
resuming `FE-P1-03` acceptance.
