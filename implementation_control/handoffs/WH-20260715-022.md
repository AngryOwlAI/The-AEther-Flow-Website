# WH-20260715-022 Runtime Requirements Project Introduction

## Boundary

- `AI-PROJECT-SYSTEM-IMPROVEMENT-INTRO-01` is checkpointed at `051457e739a99c5ea2577b659aee663711e56410` and is not replayed.
- This packet replaces only the `/ai-research-system/runtime-requirements/` raw topic paragraph with one source-grounded whole-project introduction immediately after its hero.
- Runtime, command, permission, and source rows, route cards, diagrams, and shared styles remain unchanged.
- Seven relevant authority sources are byte-identical; the live Makefile cleanup preserves runtime and permission semantics.
- Sibling routes, Resources routes, source refresh, upstream writes, push, and deployment remain forbidden.

## Required outcome

1. One substantial project-description paragraph follows the Runtime Requirements hero.
2. The paragraph identifies the two-part project before narrowing to runtime capability.
3. Exact GR remains the observable benchmark while the proposed foundation and first-principles derivation remain open.
4. Read-only, governed workflow, Python, web/browser, local-retrieval, and derivative-tool tiers are explained in public language.
5. Tool availability stays separate from tracked source, task, role, allowlist, and gate authorization.
6. Command and validator evidence stays scoped to the named checked surface rather than proof, source authority, deployment, or untested behavior.
7. Installed tools, dependencies, scripts, targets, PASS, screenshots, caches, AI output, and webpages remain operational support rather than proof or permission.
8. Writing, focused tests, provenance, build, aggregate quality, and browser review pass.

## Result

- The raw Runtime Requirements topic paragraph is replaced by one substantial general-public project introduction immediately after the hero.
- The paragraph defines the two-part program before narrowing to runtime tiers, capability versus permission, scoped command evidence, maintenance signals, and retrieval boundaries.
- Installed tools, passing commands, screenshots, caches, AI output, and webpages remain operational support rather than scientific proof or permission.
- Runtime, command, permission, and source rows, route cards, diagrams, and shared styles remain unchanged.
- The technical-writing gate passed with zero warning matches.
- Twenty-three focused positioning tests and all 148 Python tests passed.
- Provenance validates for 34 routes, and the static build produces 64 pages.
- Aggregate validation and the quality gate passed against the accepted clean source pin.
- Desktop and compact in-app browser review passed without paragraph overflow or console errors.

## Next action

Run the local checkpoint transaction for `WJC-20260715-022-A` and stop without entering another route.
