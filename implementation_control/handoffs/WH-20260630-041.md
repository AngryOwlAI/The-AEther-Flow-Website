# WH-20260630-041: GF-013 Redirects, Manifests, And Old Route Retirement

This packet authorizes the route-plumbing cleanup after all 29 greenfield
planned pages validated: old `/project/*` public paths must redirect to the
new short route model, internal route-card data must stop using the old scheme
as the primary journey, and public route/provenance manifests must match the
new route inventory.

## Boundary

GF-013 may change:

- `public/_redirects`
- `public/files/manifests/page_route_map.json`
- `public/files/manifests/page_provenance.json`
- `docs/quality/sitewide-greenfield-review-ledger.md`
- `docs/quality/sitewide-greenfield-route-retirement.md`
- `scripts/validate_page_provenance.py`
- route-card data under `src/lib/`
- route links under `src/pages/project/`, `src/pages/resources/`, and
  `src/pages/ai-research-system/`
- this packet's implementation-control records

GF-013 must not deploy, push, mutate upstream, add dependencies, delete public
assets, regenerate diagrams, refresh sources, or mark owner acceptance.

## Required Validation

- `git diff --check`
- `npm run validate:content`
- `npm run validate:links`
- `npm run validate:layout`
- `npm run validate:comprehension`
- `npm run validate:provenance`
- `npm run validate:manifests`
- `npm run validate:svg`
- `npm run build`
- `npm run validate:implementation-control`
- `.venv/bin/python -m pytest`
- Manual control-record inspection
- Redirect smoke checks and desktop/mobile browser QA for representative new
  short routes

## Completion Evidence

GF-013 completion evidence is recorded at
`implementation_control/tasks/WI-20260630-041/jobs/completions/WJC-20260630-041-A.yaml`.
The packet redirects obsolete `/project/*` public paths to the greenfield short
route model, removes obsolete `/project/*` public route records from route and
provenance manifests, updates internal route-card targets, preserves old route
source files as documented validator evidence, and keeps owner acceptance,
deployment, push, source refresh, upstream writes, public asset changes, and
diagram regeneration out of scope.
Validation PASS is operational consistency only, not source proof, claim
promotion, owner acceptance, or deployment readiness.

## Next Packet

After GF-013 is checkpointed, open `GF-014: Run Owner Review Loop And
Release-Candidate QA`.
