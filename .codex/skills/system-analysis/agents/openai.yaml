interface:
  display_name: "System Analysis"
  short_description: "Analyze AEther systems with evidence, Mermaid, and claim boundaries."
  default_prompt: "Use $system-analysis to analyze this AEther Flow system, functionality, or topic and write a Markdown analysis with an embedded styled Mermaid diagram."

policy:
  allow_implicit_invocation: true
